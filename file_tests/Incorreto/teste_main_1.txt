programa
inicio
	inteiro valor;
	real	quantidade;
	cadeia nome, produto;
	leia(valor);
	enquanto (valor > -20)
	inicio
		leia(nome, produto, quantidade);
		estoque = quantidade -12;
	fim
	
	para (valor = -8; valor <= 23; valor = valor-1)
	inicio
		escreva(quantidade-valor);
	fim
	
	real media_menos_60 = calcular_media(a, b, c, d)-60;
fim

funcao calcular_media(inteiro a, inteiro b, booleano c, real d)
inicio
	se (nao (c <> verdadeiro)) entao 
	inicio
		var variavel = "isso eh interessante";
	fim

	senao entao
	inicio
		var variavel = "hu3hu3hu3 brbrbr";
		inteiro kappa = variavel-30;
		real keepo = -30-30;
		const lol = constante;
		enquanto(verdadeiro)
		inicio
			enquanto (verdadeiro)
			inicio
				INICIO
				FIM
			fim
		fim
	fim

	caractere opa = 'o';
	opa = 'o';
fim
